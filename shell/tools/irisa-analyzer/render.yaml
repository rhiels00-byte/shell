services:
  - type: web
    name: irisa-analyzer-api
    env: node
    plan: free
    rootDir: tools/irisa-analyzer
    buildCommand: npm install
    startCommand: npm run start:api
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_MODEL
        value: gpt-4.1-mini
